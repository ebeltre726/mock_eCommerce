# ── Required ────────────────────────────────────────────────────────
NODE_ENV=development

# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=0d4a201b60d6d80955382beb9838fc4a9113aab53c43c25d0de5b60d3717355db39da1c7d7be4c3e954f5c5419aa31b2e505542f3021bd8f5da3d0a6b0657370

AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=dummy
AWS_SECRET_ACCESS_KEY=dummy

S3_BUCKET_AVATARS=avatars
S3_BUCKET_PRODUCTS=products

# Get from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_12345

DYNAMODB_TABLE=Furnituria

# ── Local dev only (leave unset in production) ───────────────────────
# Points DynamoDB client at DynamoDB Local (docker-compose)
DYNAMODB_ENDPOINT=http://localhost:8000

# Points S3 client at MinIO (docker-compose)
S3_ENDPOINT=http://localhost:9000

# ── Production only (leave unset in local dev) ───────────────────────
# Comma-separated list of allowed CORS origins, e.g. https://dxxxxxx.cloudfront.net
ALLOWED_ORIGINS=http://localhost:5500,http://localhost:3000
